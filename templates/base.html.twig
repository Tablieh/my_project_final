<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/uikit@3.6.22/dist/css/uikit.min.css"/>
			<link rel="stylesheet" href="{{ asset("css/style.css") }}">
        {% endblock %}

        {% block javascripts %}
            <script src="https://cdn.jsdelivr.net/npm/uikit@3.6.22/dist/js/uikit.min.js"></script>
			<script src="https://cdn.jsdelivr.net/npm/uikit@3.6.22/dist/js/uikit-icons.min.js"></script>
        {% endblock %}
    </head>
    <body>
    
        <div class="uk-container uk-container-large">
                    <nav class="uk-navbar-container" uk-navbar>
                        <img id="icon"src="{{ asset("image/try2.jpg") }}" alt="no-image">
                        <div class="uk-navbar-center">
                        
                            <ul class="uk-navbar-nav" class="uk-nav">
                                
                                {% if app.user %}
                                    {% if is_granted("ROLE_ADMIN") %}

                                    {% endif %}
                                    <li id="burger"><a class="uk-navbar-toggle" uk-toggle="target: #sidenav" uk-navbar-toggle-icon></a></li>
    
                                    <div id="sidenav" uk-navbar-nav="flip: true" class="uk-navbar-nav">
                                        <div class="uk-navbar-nav">
                                        
                                            <li class="uk-active"><a href="{{ path("home") }}">home</a></li>
                                            <li class="uk-active"><a href="{{ path("gallary") }}">gallery</a></li>
                                            <li class="uk-active"><a href="{{ path("activity_index") }}">activity</a></li>
                                            <li class="uk-active"><a href="{{ path("avis_index") }}">Des Avis</a></li>
                                        </div>
                                    </div>
                                    
                                    
                                    <li class="uk-active"><a href="{{ path('logout') }}">LogOut</a></li>
                                {% else %}
                                    <li class="uk-active"><a href="{{ path('app_login') }}">Connexion</a></li>	
                                    <li class="uk-active"><a href="{{ path('registration') }}">Inscription</a></li>
                                    
                                {% endif %}
                                
                                
                            </ul>
                        </div>
                    </nav>
                </div>
    
    
        {% block body %}
        {% endblock %}
    </body>
    <footer class="footer">
        <p >&copy; 2021 -Tablieh~tm Broduction !!</p>
    </footer>
</html>
