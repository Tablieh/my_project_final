{% extends 'base.html.twig' %}

{% block body %}

    <div class="form" >
        <a class="lien" href="{{ path('activitys_index') }}" > Retour </a>

        {% if editMode %}
            <h1>Modification d'un Voiture <h1>
        {% else %}
            <h1 >Ajout d'un Voiture</h1>
        {% endif %}
    
            {{ form(AjoutVoitureType) }}
    </div>
{% endblock %}